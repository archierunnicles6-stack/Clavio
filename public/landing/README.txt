Drop your exact exported images in this folder using these file names:

1) hero-dashboard.png
   - Used in the top-right hero visual block.
   - Recommended aspect ratio: around 16:10.

2) podcast-panel.png
   - Used in the middle-right "Talk for 15 minutes" visual block.
   - Recommended aspect ratio: around 16:10.

Notes:
- The page already has matching placeholder UI blocks, so if images are missing the layout still looks complete.
- As soon as these files exist, they will be loaded automatically with no further code changes.
